import { StyleSheet, Text, View } from "react-native";
import React from "react";
import SubscribedCourseScreen from "@/screens/subscribed-courses/subscription.screen";

const index = () => {
  return <SubscribedCourseScreen />;
};

export default index;

const styles = StyleSheet.create({});
