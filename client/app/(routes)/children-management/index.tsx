import { StyleSheet, Text, View } from "react-native";
import React from "react";
import ChildManagementScreen from "@/screens/children-managent/childManagement.screen";

const index = () => {
  return <ChildManagementScreen />;
};

export default index;

const styles = StyleSheet.create({});
