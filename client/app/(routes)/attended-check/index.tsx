import { StyleSheet, Text, View } from "react-native";
import React from "react";
import AttendedScreen from "@/screens/attended/attended.screen";

const index = () => {
  return <AttendedScreen />;
};

export default index;

const styles = StyleSheet.create({});
